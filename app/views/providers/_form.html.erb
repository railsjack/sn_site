<%= simple_form_for([@lovedone,@provider]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :company %>
    <%#= f.association :lovedone %>
  </div>

  <div class="form-actions">
    <%= f.button :submit , class: 'btn btn-success btn-md', value: 'Add Service Provider' %>
  </div>
<% end %>
